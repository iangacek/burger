<h1>Eat-Da-Burger!</h1>

<ul>{{#each burgers}}
    <li>
        <p>ID: {{this.id}}</p>
        <p>Burger: {{this.burger}}</p>
    </li>
</ul>

<form action="/" method="POST">
    <textarea type="text" name="burger"></textarea>

    <button type="submit">Submit-Da-Burger</button>
</form>